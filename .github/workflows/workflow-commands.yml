name: "workflow commands"
on: ['push']
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: group logging
        runs: |
          echo: "::group::My group Message"
          echo: "Msg 1"
          echo: "Msg 2"
          echo" "::endgroup::"